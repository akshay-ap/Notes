<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_edit_note"
        android:icon="@mipmap/icon_edit"
        android:title="Edit Note"
        app:showAsAction="always"/>
    <item
        android:id="@+id/action_delete_note"
        android:icon="@mipmap/ic_delete_forever_black_24dp"
        android:title="Delete Note"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_add_remainder"
        android:icon="@mipmap/icon_add_alarm"
        android:title="Delete Note"
        app:showAsAction="always"/>
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_settings_1"
        android:title="Settings"
        app:showAsAction="never"/>

</menu>